services:
  backend:
    build:
      context: ./Backend
    container_name: book_backend
    ports:
      - "8000:8000"
    volumes:
      - book_data:/app/data

  frontend:
    build:
      context: ./Client
    container_name: book_frontend
    ports:
      - "5173:5173"

volumes:
  book_data: